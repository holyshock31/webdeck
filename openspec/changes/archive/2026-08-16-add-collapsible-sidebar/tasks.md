# Tasks — add-collapsible-sidebar

- [x] 侧边栏头部增加收起/展开按钮（沿用 icon-btn 样式），点击切换收起状态
- [x] 注册 ⌘\ 快捷键切换收起/展开，与按钮行为一致
- [x] 渲染层实现收起布局：收起时隐藏侧边栏、主内容区占满窗口，展开时恢复原宽度（styles.css 增加状态类）
- [x] 收起状态下在窗口左缘显示浮动展开按钮，保证收起后仍可展开
- [x] 收起/展开状态持久化：settings.sidebarCollapsed 随 webdeck.json 原子写入，重启恢复，缺失时默认展开
- [x] 收起按钮与浮动展开按钮样式接入主题变量，dark / light 主题下均可辨识
- [x] 手动验证：按钮与 ⌘\ 切换、重启后状态保持、收起后快捷键切应用与浮动按钮展开、收起期间状态灯更新正常
